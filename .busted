-- busted configuration: lets you simply run `busted` from the repository root.
-- ROOT = specs directory; lpath = allows require("tests.support.wowenv").
return {
    default = {
        ROOT = { "tests/spec" },
        lpath = "./?.lua;./?/init.lua",
        verbose = false,
    },
}
